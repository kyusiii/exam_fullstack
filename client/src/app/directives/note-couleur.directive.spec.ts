import { NoteCouleurDirective } from './note-couleur.directive';

describe('NoteCouleurDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteCouleurDirective();
    expect(directive).toBeTruthy();
  });
});
